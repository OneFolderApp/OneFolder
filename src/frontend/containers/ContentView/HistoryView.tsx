import React from 'react';
import { GalleryProps } from './utils';

const ListGallery = ({ contentRect, select, lastSelectionIndex }: GalleryProps) => {
  return <div className="">History view</div>;
};

export default ListGallery;
